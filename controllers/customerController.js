const pool = require('../db');

const getCustomers = (req, res) => {
  // get data from db
};

const addCustomer = (req, res) => {
  // insert customer 
};

module.exports = {
  getCustomers,
  addCustomer
}